spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  application:
    name: common-basedata
  cloud:
    config:
      uri: https://config.i-facture.com
      label: master
      profile: dev
---
spring:
  profiles: test
  application:
    name: common-basedata
  cloud:
    config:
      uri: https://config.i-facture.com
      label: master
      profile: test
---
spring:
  profiles: pro
  application:
    name: common-basedata
  cloud:
    config:
      uri: http://localhost:6101
      label: master
      profile: pro